#include "header.h"

int main(){

    // readfile
    string s = readFile("input_sample.txt");\
    int s_length = s.length() + 1;

    char charValue[s_length];
    int alphabetFreq[100][1];

    for(int i = 0 ; i < s_length; i++){
        for(int j = 0; j < 100; j++){
            if(charValue[i] == )
        }
    }
    
    

    

    

    






}